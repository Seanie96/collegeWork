#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(int argc, char * argv[]) {

  int index = 0;
  char* token = malloc(sizeof(char) * 100);
  token = strtok(argv, ", (");
  while(token.length >= ) {

    

  }

}
